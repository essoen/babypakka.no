jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: none

micronaut:
  security:
    token:
      jwt:
        signatures:
          secret:
            generator:
              secret: "test-secret-for-unit-tests-only"
